#pragma once

/** Feed battery / node health / vision / dock into shared pose engine (parallel to behavioral emotion_engine). */
void walleEmotionPoseBridgeInit(void);
void walleEmotionPoseBridgeTick(void);
